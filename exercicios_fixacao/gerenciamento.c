/*Crie um programa que permita ao usuário adicionar contatos à lista de contatos. Use alocação dinâmica para armazenar os dados dos contatos, incluindo nome, número de telefone e endereço de e-mail. O programa deve permitir que o usuário visualize a lista de contatos, adicione novos contatos e, opcionalmente, remova contatos existentes.*/
#include <stdio.h>
#include <stdlib.h>
struct Contatos
{
    char nome[20];
    char endereco[20];
    int telefone;
    char email[20];
};


int main(int argc, char const *argv[])
{
    struct Contatos meusContatos;
    int *arrayContato;
    
    arrayContato = (* int)
    return 0;
}
